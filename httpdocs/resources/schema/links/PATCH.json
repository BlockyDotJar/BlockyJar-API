{
    "type": "object",
    "properties": {
      "link": { "type": "string", "format": "uri" },
      "expires_on": { "type": "string", "format": "date" },
      "delete_expired_details": { "type": "boolean" }
    },
    "anyOf": [
        {
            "required": [
                "link"
            ]
        },
        {
            "required": [
                "expires_on"
            ]
        },
        {
            "required": [
                "delete_expired_details"
            ]
        }
    ]
}